# Changelog

## 0.1.0 — 2026-04-19

Initial import from `~/.claude/skills/rm/`. 26 skills live and in daily use since Mar 26.

**Skills included:**

Dev: `/review`, `/ship`, `/plan`, `/cso`, `/extract-test`, `/pdf`
Ops: `/sweep`, `/standup`, `/priorities`, `/state`, `/pr-status`, `/ticket-hygiene`, `/draft`
Strategy: `/competitor-scan`, `/client-recon`, `/fcm-demo`
Safety: `/careful`, `/freeze`, `/guard-truthly`, `/verify`
Meta: `/feedback-audit`

Shared references: `memory/correction_tracker.md`, `patterns/anti_rationalization.md`, `patterns/mandatory_validation.md`, `patterns/session_state.md`, `CLAUDE.md`.

**Provenance:** gstack-derived skills (/careful, /freeze, /review, /ship, /plan, /cso) carry "Adapted from" footers. RM-specific skills are net-new.
